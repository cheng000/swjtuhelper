<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a201711116">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:windowSoftInputMode="adjustPan|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginedActivity" />
        <!--<activity android:name=".fragment.NewsActivity" />-->
        <!--<activity android:name=".fragment.ExamActivity" />-->
        <!--<activity android:name=".fragment.ScheduleActivity" />-->
        <activity android:name=".SettingActivity" />
        <activity android:name=".NewsLookActivity" />
        <activity android:name=".notice.NoticeNewsActivity" />
        <activity android:name=".ExamLookActivity" />
        <activity android:name=".NotebookActivity"></activity>
    </application>

</manifest>